let resultado = document.getElementById("resultado")
let cantidadDeSegundos = 300

for (cantidadDeSegundos = 300; cantidadDeSegundos >= 0; cantidadDeSegundos--) {
    resultado.innerHTML = `${cantidadDeSegundos} segundos`
}
